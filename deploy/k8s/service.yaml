apiVersion: v1
kind: Service
metadata:
  name: webgame
  namespace: lurus-webgame
  labels:
    app: webgame
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    app: webgame
